[project]
name = "graphwar"
version = "0.1.0"
description = "Detect player and target coordinates in Graphwar screenshots"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.0",
    "opencv-python-headless>=4.13.0.92",
    "pillow>=12.2.0",
    "sympy>=1.14.0",
]
